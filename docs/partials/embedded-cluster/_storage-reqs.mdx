Embedded Cluster stores data in `/var/lib/embedded-cluster`, which must be writable.

- Used to store your installer
- Includes:
  * Temporary files
  * Supporting binaries (for example, kubectl and Replicated’s preflight and support-bundle plugins)
- Should be large enough to fit at least 2 installer binaries
at least 40 Gi and less than 80% full

#### Additional Directories and Files

In addition to the primary directories above, Embedded Cluster creates directories and files in the following locations:

- /etc/cni
- /etc/k0s
- /opt/cni
- /opt/containerd
- /run/calico
- /run/containerd
- /run/k0s
- /sys/fs/cgroup/kubepods
- /sys/fs/cgroup/system.slice/containerd.service
- /sys/fs/cgroup/system.slice/containerd.service
- /sys/fs/cgroup/system.slice/k0scontroller.service
- /sys/fs/cgroup/system.slice/k0scontroller.service
- /usr/libexec/k0s
- /var/lib/calico
- /var/lib/cni
- /var/lib/containers
- /var/lib/kubelet
- /var/log/calico
- /var/log/containers
- /var/log/embedded-cluster
- /var/log/pods
- /usr/local/bin/k0s
