:::note
If your release supports installations with Replicated KOTS, `--yaml-dir` is required. If your release supports installations with the Helm CLI only, either `--yaml-dir` or `--chart` is required.
:::