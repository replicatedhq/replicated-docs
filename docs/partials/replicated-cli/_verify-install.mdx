Verify that the installation was successful:

  ```
  replicated version
  ```

To begin using the replicated CLI, authenticate with an API token and your application slug. See [Authenticate with Application Slug and API Token](#auth) below.  