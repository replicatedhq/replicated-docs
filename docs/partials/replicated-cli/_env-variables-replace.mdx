Replace:

  - `APP_NAME` with the name of your application, as shown in the URL path at https://vendor.replicated.com/apps. The application name is case sensitive.
  - `TOKEN` with the service account token or user API token you created in the vendor portal.