Replace:

  - `APP_SLUG` with the slug for the target application. The application slug is case sensitive.
  - `TOKEN` with the service account token or user API token you created in the vendor portal.