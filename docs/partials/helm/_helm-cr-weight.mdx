Determines the order to apply charts. Applies only when the HelmChart custom resource has `useHelmInstall: true`. Charts are applied by weight in ascending order, with lower weights applied first. **Supported values:** Positive or negative integers. **Default:** `0`

For more information, see [Defining Installation Order for Native Helm Charts](/vendor/helm-native-helm-install-order).
