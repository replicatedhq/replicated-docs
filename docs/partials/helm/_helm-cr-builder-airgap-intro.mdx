To create an `.airgap` bundle for a release that contains Helm charts, the Replicated vendor portal renders the Helm charts' templates with `helm template`. The `builder` key specifies the Helm chart values that the vendor portal uses when running `helm template` to create the `.airgap` bundle.