- Add charts to applications. See [Including Optional Charts](helm-optional-charts) and the [Charts](https://helm.sh/docs/topics/charts/) in the Helm documentation.
- Include value keys. See [Including Optional Value Keys](helm-optional-value-keys).
- Define an installation order for multiple Helm charts. See [Defining Installation Order for Native Helm Charts](helm-native-helm-install-order).
