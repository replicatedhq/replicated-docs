```yaml
apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app: nginx
  annotations:
    kots.io/when: repl{{ or (eq Distribution "embedded-cluster") (eq Distribution "kurl")}}
spec:
  type: ClusterIP
  ports:
    - port: 80
  selector:
    app: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    app: nginx
  annotations:
    kots.io/when: repl{{ or (eq Distribution "embedded-cluster") (eq Distribution "kurl")}}
spec:
  type: NodePort
  ports:
    - port: 80
      nodePort: 8888
  selector:
    app: nginx
```    