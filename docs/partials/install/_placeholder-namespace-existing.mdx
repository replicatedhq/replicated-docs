* `NAMESPACE` with the namespace where you want to install both the application and the app manager. By default, the `kots install` command installs the application and the app manager in the `default` namespace.