* `NAMESPACE` with the namespace where the Kubernetes installer installed the app manager when provisioning the cluster. By default, the Kubernetes installer installs the app manager in the `default` namespace.