When you use the kots CLI to install an application in a Kubernetes installer cluster, you install the application in the same namespace where the Kubernetes installer previously installed the Replicated app manager.