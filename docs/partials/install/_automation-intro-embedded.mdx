When you use the kots CLI to install an application in a Kubernetes installer cluster, you install the application in the same namespace where the Replicated app manager is installed. You previously installed the app manager when you ran the Kubernetes installer script to provision the cluster.