KOTS deploys the Admin Console, which provides a user interface for installing and managing applications. After you install KOTS, you will use the Admin Console to install the application in your cluster in the [Install and Deploy the Application](#install-app) procedure that follows.