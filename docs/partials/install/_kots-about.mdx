The app manager deploys the Replicated admin console, which provides a user interface for installing and managing applications. You will use the admin console to install the application in your cluster in [Install and Deploy the Application](#install-app) below.