This procedure describes how to use kURL to provision an embedded cluster on a VM or bare metal server. Additionally, kURL automatically installs Replicated KOTS in the `default` namespaces in the cluster.