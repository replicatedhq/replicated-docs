After you install the app manager, log in to the admin console to provide your license file and the `.airgap` bundle, define your configuration values, run preflight checks, and install and deploy the application.