<li><p><code>ports.applicationUrl</code>: (Optional) The URL used by links to the port-forwarded service from the admin console dashboard. <code>ports.applicationUrl</code> must match a service found in the Kubernetes Application manifest in the release. For example, <code>ports.applicationUrl: "http://my-application-url"</code> in the KOTS Application custom resource must match <code>descriptor.links.url: http://my-application-url</code> in the Kubernetes Application manifest.</p></li>