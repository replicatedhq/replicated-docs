# replicated notification event-type ls

List notification event types

```
replicated notification event-type ls [flags]
```

### Aliases

```
ls, list
```

### Options

```
  -h, --help            help for ls
      --limit int       Maximum results to request from the API (0 means no limit)
  -o, --output string   The output format to use. One of: json|table (default "table")
      --q string        Search query
```

### Options inherited from parent commands

```
      --app string       The app slug or app id to use in all calls
      --debug            Enable debug output
      --profile string   The authentication profile to use for this command
      --token string     The API token to use to access your app in the Vendor API
```

### SEE ALSO

* [replicated notification event-type](replicated-cli-notification-event-type)	 - List available notification event types

