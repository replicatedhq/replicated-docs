import GlobalFlags from "../partials/replicated-cli/_global-flags.mdx"

# default clear-all

Clear all default values used by other commands run by the current user.

## Usage

```bash
replicated default clear-all [flags]
```

## Global Flags

<GlobalFlags/>

## Example

```bash
# Clear all defaults
replicated default clear-all
```